defaults:
  - vicreg_geo_frame_transformer_v2
  - _self_

# Exact reflection-invariant ablation. This evaluates both local parity
# hypotheses with shared weights and averages them before the transformer.
experiment_name: VICREG_GEOFRAME_V2_O3_l${latent_size}_N${data.num_points}_M${data.model_points}_${encoder.name}

encoder:
  kwargs:
    parity_mode: invariant
