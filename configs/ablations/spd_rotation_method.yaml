experiment:
  config_name: spd_synth_small
  config_path: configs
  base_overrides:
    - wandb_mode=online
    - log_every_n_steps=10
    - epochs=800
variable:
  override: rotation_mode
  values: [matrix_head, inv_no_rot, inv_kabsch]
metrics:
  final:
    - train/loss
    - train/chamfer
    - val/loss
    - val/chamfer
    - val/emd
    - val/phase/nmi
    - val/phase/ari
    - val/phase/silhouette
    - train/phase/nmi 
    - train/phase/ari
  best:
    - name: val/loss
      mode: min
    - name: val/phase/nmi
      mode: max
    - name: val/emd
      mode: min
    - name: val/phase/silhouette
      mode: max
    - name: val/phase/ari
      mode: max
output:
  root_dir: output/ablations
  run_name: spd_rotation_method
