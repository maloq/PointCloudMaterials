# Install on the existing pointnet GPU environment with --no-deps. Upstream
# GATr's historical numpy/opt_einsum constraints predate this tested stack.
# Tensor masks dispatch to upstream PyTorch SDPA, not xformers custom kernels.
GATr @ git+https://github.com/Qualcomm-AI-research/geometric-algebra-transformer.git@6afc26f26b8fcf51136ae8c1d264a36e14b6e497
einops==0.8.2
xformers==0.0.35
