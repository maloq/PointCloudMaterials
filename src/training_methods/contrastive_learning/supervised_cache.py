"""Legacy import path for retained research and saved objects."""

from src.training_methods.shared.supervised_cache import (
    _as_string_list,
    _as_int_mapping,
    _parse_optional_eval_k,
    _normalize_method_name,
    _is_kmeans_plus_plus_method,
    _ensure_kmeans_plus_plus_method,
    _to_finite_float,
    _format_label_histogram,
    _validate_cached_supervised_arrays,
    _primary_kmeansplusplus_hungarian_key,
    _stabilize_class_metric_keys,
    _random_rotation_matrices,
    _flatten_features,
    _prepare_features_and_labels,
    _compute_linear_svm_accuracy,
    _compute_linear_svm_train_to_test_accuracy,
    _sample_probe_rows,
    _resolve_probe_k,
    _probe_cluster_entropy,
    _resolve_probe_best_k_range,
    _compute_probe_best_num_clusters,
    _compute_probe_metrics,
    _build_supervised_eval_loader,
    _collect_split_supervised_features,
    _extract_rotated_supervised_features_from_batch,
    _collect_rotated_split_supervised_features,
    _compute_rotated_test_accuracy_metrics,
    _compute_train_to_test_svm_accuracy,
    _infer_stage_acc_settings,
    _infer_label_cluster_count,
    _get_stage_dataset,
    _infer_dataset_class_count,
    _resolve_hungarian_eval_k,
    init_supervised_cache,
    reset_supervised_cache,
    cache_limit_for_stage,
    cache_supervised_batch,
    log_supervised_metrics,
    _gather_latents_labels_ddp,
)
