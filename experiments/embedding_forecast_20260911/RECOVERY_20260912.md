# Forecast preparation recovery — September 12, 2026

Question: recover the existing full-scale comparison after preparation exhausted
the `/home/ids` user quota, retaining the completed work and the 32-epoch protocol.

## Diagnosis

The first H100 allocation 988041 reached its time limit after eight shards. A40
continuation 990061 completed through shard 034, then failed on September 12 at
02:08 Europe/Paris with `OSError: [Errno 122] Disk quota exceeded` during mmap
flush. H100 preparation job 989350 retried at 15:42 and failed at 15:43 while
opening the same incomplete shard. Its `embeddings.npy` was left empty. There are
35 completed shards out of 125, with completion manifests and checksums.

Training jobs 990063 and 990064 have no execution records, logs or output fits;
their failed preparation dependency prevented them from starting. Their collector
990065 also did not run. All four are absent from the live queue. Slurm accounting
is unavailable because its database connection is refused; diagnosis uses the
saved commands, logs, run records and dependency receipts.

## Storage correction

The user requested regenerable cache deletion first, then float16 conversion if
needed. The selected cleanup removes 229 train/validation `clouds.npy` arrays
(25.353 GiB) from the completed temporal-hypotheses campaign. Original source
manifest identities and converted-cache checksums are verified before deletion.
Al and Mg sample neighborhoods were regenerated and matched the stored float16
values exactly. Test clouds, source trajectories, IDs, motion targets, scalers,
selected checkpoints, evaluation results and source snapshots are retained.
The Ta caches are retained because their source manifest identities changed.

The remaining forecast cache is converted to float16, saving another 13.689 GiB
in its 35 completed shards. The final 125-shard cache requires 48.889 GiB instead
of 97.778 GiB; 35.200 GiB is needed after conversion. Together these corrections
leave about 3.84 GiB headroom relative to the previously exhausted quota, excluding
small metadata and other storage changes. NFS quota RPC is unavailable, so this
is a byte budget rather than a reported quota allowance.

Float16 is an explicit change to stored forecast inputs and targets. Model windows
still decode to float32; scaling accumulates float64 moments of the rounded
training embeddings. Scientific source selection, encoder checkpoint, splits,
model architecture, seed, augmentation, loss, 32 epochs and GPU resources are
retained. Training had not begun, so there is no checkpoint continuation across
the precision change. Simulation coordinates and integration/restart state are
unchanged. Historical logs and source snapshots remain immutable.

## Implementation and verification

The maintained `convert_trajectory.py embedding-cache` workflow verifies each
rounded embedding value and all non-embedding checksums, retains the original
protocol and shard manifests, and publishes the new dtype and producer identity.
Preparation and cache readers reject an unfinished migration. Interrupted array
replacement can be resumed using the exact same audit and configuration.

The restart snapshot copies the previously queued 32-epoch source tree and changes
only `src/training_methods/embedding_forecast/data.py` for explicit storage dtype
and migration-state handling. Both old source snapshots were checked against all
306 recorded file hashes and matched. The new storage workflow and forecast,
cache-conversion and research-layout tests passed: **45 tests** (43 in the combined suite, plus two added preparation-storage cases). Metric definition
hashes were updated; the metric formulas are unchanged.

A real encoder smoke test on the next source (035, 1,024 centers) produced finite
256-channel float16 embeddings on A40. Maximum absolute rounding error was
0.00048804 and RMS error was 0.00002508 in encoder-output units. These are storage
checks, not trained forecast scores. Aggregate conversion errors are recorded in
the conversion audit.

## Reproduction and artifacts

Configuration: [recovery_float16_config.json](technical/recovery_float16_config.json).
Concrete restart commands and dependency names:
[recovery_restart_plan.json](technical/recovery_restart_plan.json).
Run root: `output/embedding_forecast/scale-recovery-20260912/`.
Operational records, logs, source snapshot and scripts live in its `technical/`.
Forecast fits retain the original `output/embedding_forecast_20260911/scale/runs/`
output location. The cache stays at
`/home/ids/vmorozov/training-cache/embedding-forecast-full-20260911`.

```bash
python scripts/convert_trajectory.py embedding-cache \
  --config output/embedding_forecast/scale-recovery-20260912/technical/config.json \
  --producer output/embedding_forecast/scale-recovery-20260912/technical/source/src/training_methods/embedding_forecast/data.py \
  --audit output/embedding_forecast/scale-recovery-20260912/technical/embedding-conversion.json
```

Do not rerun the migration while preparation or training uses the cache.
The cleanup plan, per-file deletion audit and preserved reconstruction metadata
are in the recovery run's `technical/`. Its disposable `rebuild_temporal_clouds.py`
calls the maintained producer with the saved identities and configuration and
requires an exact SHA256 match before restoring any reclaimed array.

New source files are maintained conversion implementation and regression tests.
This report and the two JSON files are experiment records. Generated source/job
snapshots, storage inventories, probes and reconstruction code are run artifacts
or disposable diagnostics.

## Replacement submission

Submitted and verified on September 12, 2026:

| Stage | Slurm job | Dependency |
| --- | --- | --- |
| prepare | 990768 | GPU availability |
| path_ar_large_aug-s20260911-e000 | 990769 | 990768 |
| path_direct_large_aug-s20260911-e000 | 990770 | 990768 |
| collect | 990771 | 990769, 990770 |

[Authoritative submission receipt](../../output/embedding_forecast/scale-recovery-20260912/technical/submission.json). At verification, preparation was queued for an H100 and both fits and collection were pending dependencies. No restarted training results are claimed. All 35 shards were converted and verified; aggregate RMS rounding error was 2.5311229489673055e-05, and maximum absolute error was 0.00048828125 in encoder-output units. Total reclaimed storage was 39.042 GiB.
