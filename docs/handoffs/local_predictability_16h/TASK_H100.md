# Local task: one H100, one seed, shared 16-hour ceiling

This is the local execution handoff, not a running job. Follow
[the common operations plan](../../local_predictability_16h.md) and
[scientific protocol](../../../experiments/local_predictability_20260917/README.md).
Use seed 20260919 only. Query the actual allocation and reserve the last hour;
never assume the historical node/job identity still holds.

1. Reaggregate the old positive control in a fresh output using retained
   predictions, its exact local config and original folds/weights. Audit the
   150-source cohort and crosswalk legacy model-training lineages. Freeze
   outcome-independent center selections, validation halves, origins and hashes.
2. Build full-timeline PTM labels for 16 core centers first; reuse verified
   64-center labels where available. Audit patch/full-cell agreement, confirmation
   boundaries, censoring and causal inputs. Report source/event/window coverage.
   The larger census must not delay core results. No new simulation campaign.
3. Produce the 16-center descriptor timeline, current/future observability audit
   and train-only normalization; exchange compact row/target/hash releases with
   H200. Raw trajectories are already there. Validate extra center/shell feature
   definitions before their auxiliary descriptor jobs.
4. Fit simple and nonlinear descriptor hazard baselines (one seed), physical
   ridge references and persistence. Match population rows when comparing to
   native models. Freeze checkpoint selection independently from calibration;
   never select based on test results. Publish this report as soon as ready.
5. Receive H200's tested batching/head/gating patch and verify parity locally.
   Freeze source code and agree a shared feasible K before either native group.
   After coverage/assay/fitting gates, train the supervised-onset snapshot parent
   then three equal-K continuations: snapshot, real 12-ps history, repeated current
   frames. Keep the same seed, effective batch and source sampling. Do not reopen
   width, multi-seed or smoothness sweeps.
6. Merge H200's physical-mean predictions and frozen-state diagnostics into a
   paired report, preserving the distinction between label-free and supervised
   objectives. Bootstrap whole sources, not neighboring center windows. Calibrate
   alarms on calibration sources; report misses in timing. Time dense native
   inference before promising it; missing dense scores must not be interpolated.
7. If setup misses hour 4 or a full matched group cannot fit, report the feasible
   remainder and finish the audited baseline/diagnostic result. Target first
   complete report by hour 12; hard ceiling 16 hours or allocation end, whichever
   comes first. Export code/config hashes, metric definitions and incomplete-work
   ledger. Optional H48, shooting, precision and sufficiency extensions are off.

Once implementation and executable queue are validated, launch detached with the
existing tracking/allocation workflow and explicit dependencies. This planning
handoff itself does not authorize a hidden background launch.
