# Finish the 520 K sources — 2026-09-13

User clarified that the requested remaining top-ups are the independent 520 K
sources, not the superseded 48 ps shooting branches. Direct manifest/outcome audit
found 510 K complete (30/30) and 520 K incomplete (6/30). Missing indices are 33–56.

Submitted detached Slurm array **991371**, six workers of four sources each, at most
three workers concurrently. Each source uses its original 48 MPI ranks, seeds,
independent melt, equilibration and 600 ps measurement protocol. Allocation per
worker: CPU partition, 32 GiB RAM, 36-hour walltime. The existing high-temperature
source command performs structural validation, PTM analysis and verified float16
conversion, preserving exact final and melt restart files.

New run root:
`/scratch/PERSO/vmorozov/PointCloudMaterials/simulations/al_520K_remaining24_20260913T204114Z`.
The sibling `-launch` directory holds the recovery plan, generated batch script,
submission receipt, logs and finalization lock. Immutable source manifest, initial
configuration and per-source input files were copied and checked byte-for-byte.
The 36 previously completed sources are linked from the original collection.
Only the 24 unfinished sources are rerun; old interrupted outputs remain intact at
original WORK paths. This is a recovery view of the same source identities, not
24 additional independent samples beyond the intended 30 per temperature.

After every worker succeeds, the last finisher takes a lock, calls the existing
full source summarizer (including checksum verification), then publishes the new
collection to STORE using `project.py publish-simulation --move`. Failure leaves
SCRATCH data and exact restart state available for diagnosis; no periodic monitoring
or automatic restart is enabled. Original WORK outcomes remain a historical view;
use the recovery root for updated 520 K completion counts.

Reproduce task execution with the existing command inside an appropriate allocation:

```bash
python -m src.simulation.campaigns.independent_meam_high_temperature run-task \
  --campaign-root RECOVERY_ROOT --task-index INDEX
```

The accepted 15 ps shooting top-up remains complete (160/160); the old unfinished
48 ps top-up was not relaunched.
